# live-location-tracker
A real-time mobile location tracking system built with Python (Flask), Socket.IO, Leaflet Maps, and an Android app using FusedLocationProvider. Tracks device live GPS positions and displays them on an interactive dashboard.
